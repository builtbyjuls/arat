spring.application.name=arat
spring.datasource.url=${ARAT_DATABASE_URL}
spring.datasource.username=${ARAT_DATABASE_USERNAME}
spring.datasource.password=${ARAT_DATABASE_PASSWORD}
logging.structured.format.console=logstash
